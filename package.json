{
  "name": "@ubc-biztech/sdk",
  "version": "0.1.0",
  "description": "Typed, chained client for BizTech club data, generated from a declared ontology.",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ubc-biztech/sdk.git"
  },
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "docs",
    "guides",
    "AGENTS.md",
    "README.md"
  ],
  "publishConfig": {
    "registry": "https://npm.pkg.github.com"
  },
  "scripts": {
    "gen": "tsx src/gen/index.ts",
    "check:semver": "tsx src/check/semver.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "build": "rm -rf dist && tsc -p tsconfig.build.json",
    "prepare": "npm run build",
    "test": "vitest run",
    "test:contract": "BT_CONTRACT=1 vitest run test/contract.test.ts",
    "ci": "npm run gen && npm run typecheck && npm test && npm run build && git diff --exit-code -- src/client/generated docs",
    "check": "tsx src/tools/check.ts",
    "new": "tsx src/tools/new.ts"
  },
  "peerDependencies": {
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vitest": "^3.0.0",
    "zod": "^4.0.0"
  }
}
