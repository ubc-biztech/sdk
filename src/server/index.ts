/**
 * @ubc-biztech/sdk/server — what a generated backend service imports.
 *
 * ```ts
 * import { createHandler, ActionError, type Impl } from "@ubc-biztech/sdk/server/judging";
 * export const api = createHandler(impl);
 * ```
 */
export { ActionError, createRouter, satisfies, type Ctx, type Principal, type RouteMeta, type Roles, type ApiGatewayEvent, type ApiGatewayResponse } from "./runtime.js";
